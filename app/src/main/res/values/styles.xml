<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backForDialogsWindowItem</item>
        <item name="colorPrimaryDark">@color/backForDialogsWindowItem</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.MaterialComponents.Dialog.Alert">
        <item name="android:windowBackground">@color/backGround</item>
        <item name="android:actionBarItemBackground">@color/backGround</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Body1</item>
        <item name="android:colorBackground">@color/backGround</item>
        <item name="android:colorButtonNormal">@color/backGround</item>
        <item name="android:panelColorBackground">@color/backGround</item>
        <item name="android:textSize">20dp</item>
    </style>
    
    <style name="ToolBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/backForDialogsWindowItem</item>
        <item name="android:colorPrimary">@color/backForDialogsWindowItem</item>
        <item name="android:colorPrimaryDark">@color/backForDialogsWindowItem</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/backForDialogsWindowItem</item>
    </style>

</resources>
